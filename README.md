
  # Healthcare Web App UI Design

  This is a code bundle for Healthcare Web App UI Design. The original project is available at https://www.figma.com/design/2b9QG0y7yKSdXMXQVk3Fqo/Healthcare-Web-App-UI-Design.



  ## Running the code

  Run `npm install` to install the dependencies.

  Run `npm run dev` to start the development server.

  Open new terminal 

  Run all these commands 
  
## Backend setup (Windows)

```powershell
cd Diabetes_risk_prediction
python -m venv venv
venv\Scripts\activate
pip install --upgrade pip
pip install -r requirements.txt
python app.py
